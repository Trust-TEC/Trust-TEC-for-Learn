const colors = require('tailwindcss/colors')
module.exports = {
  content: ['./public/index.html'],
  theme: {
    container: {
      center: true,
    },
    extend: {
      colors:{
      
 
      },
    },
  },
  plugins: [],
}
